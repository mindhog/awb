## UI Module for class midi tracker interface.

import crack.ascii radix;
import crack.cont.array Array;
import crack.cont.hashmap HashMap;
import crack.exp.termui Event, GraphicsContext, KeyEvent, Window;
import crack.functor Functor2;
import crack.io cerr, FStr;
import crack.midi.event NoteEvent, MidiEvent = Event, Track;
import crack.strutil rjust;
import crash.crash Builtin, EvalContext, ObjOf, ParmInfo;
import crash.types IntObj;
import deadly.util convertKey;
import .engine engine;
import .model MidiTrack, OmniTrack;
import .textui BindCmd;
import .tel makeTrackObj, makeMutMidiTrackObj, MutMidiTrackObj, TrackObj;

@import crack.ann cvars, define, nullable;
@import crash.ann function;

## Tracker-style midi editor.
class Tracker : Window {

    byte
        backgroundAttr = 0,
        channelAttr = 2,
        noteAttr = 6,
        velAttr = 3,
        timeAttr = 6,
        punctAttr = 3,
        selectedAttr = 0x40;

    Track track;

    # Pulses per beat.
    int __ppb = engine.getPPB();

    # Steps per beat.  In regular mode, this is the number of rows that we
    # will render for each beat.
    int __spb = 4;

    # If true, events are rendered more compactly with the next row containing
    # events from the next point in time (the default is tradtional tracker
    # style, each row corresponds to an interval in the sequence so rows
    # without events would be empty.
    bool __irregular;

    # top row on display.
    int __top;

    # Selected row and column.
    int __curRow, __curCol;

    # Currently selected midi event, null if no event is currently selected.
    # This is updated by paint(), which is not ideal.
    MidiEvent __selectedEvent;

    HashMap[int, Functor2[bool, Tracker, Event]] __callbacks = {};

    oper init(Window parent, Track track) : Window(parent), track = track {}

    void bind(int key, Functor2[bool, Tracker, Event] callback) {
        __callbacks[key] = callback;
    }

    bool onEvent(Event event) {
        if (ke := KeyEvent.cast(event, null)) {
            cb := __callbacks.get(convertKey(ke));
            if (cb)
                return cb(this, event);
        }
        return false;
    }

    uint __getTrackEnd() {
        return track ? track[-1].time : 0;
    }

    # Returns pulses per step.
    @define __getPPS() { __ppb / __spb }

    ## Returns the total number of steps represented by the track.
    uint __getSteps() {
        frames := __getTrackEnd();
        pps := @__getPPS();
        steps := frames / pps;
        if (frames % pps)
            steps += 1;
        return steps;
    }

    void __renderEvent(GraphicsContext gc, int row, int col, MidiEvent event,
                       bool selected) {
        String zpadx(int value) {
            return rjust(radix(value, 16), 2, b'0');
        }

        battr := selected ? selectedAttr : 0;

        if (n := NoteEvent.cast(event, null)) {
            gc.write(col, row, channelAttr | battr,
                     radix(n.channel, 16)
                     );
            gc.write(col + 1, row, noteAttr | battr, zpadx(n.note));
            gc.write(col + 3, row, velAttr | battr, zpadx(n.velocity));
        }
    }

    @nullable MidiEvent oper .selectedEvent() { return __selectedEvent }

    void pageDown() {
        if (__top + height < __getSteps())
            __top += height;
    }

    void pageUp() {
        if (__top >= height)
            __top -= height;
        else
            __top = 0;
    }

    void down() {
        if (__curRow < __getSteps() - 1) {
            ++__curRow;

            if (__curRow >= __top + height)
                __top = __curRow - height + 1;
        }
    }

    void up() {
        if (__curRow > 0) {
            --__curRow;

            if (__curRow < __top)
                __top = __curRow;
        }
    }

    void right() {
        # We don't have to check on this, it will get bound to the end during
        # paint.
        __curCol += 1;
    }

    void left() {
        if (__curCol > 0)
            --__curCol;
    }

    void paint() {
        gc := getGraphicsContext();
        gc.fillRect(0, 0, width, height, backgroundAttr, b' ');

        # Zero-pad the value to make it at least 'size' characters.
        String zpad(int value, int size) {
            return rjust(radix(value, 10), size, b'0');
        }

        if (__irregular) {
            gc.write(0, 0, 7, 'Irregular mode not implemented yet.');
        } else {
            # Compute pulses per measure (assume 4 beats per measure if not
            # yet defined)
            ppm := engine.getCurrentSection().measure;
            ppm = ppm ? ppm : 4 * __ppb;
            steps := __getSteps();
            pps := @__getPPS();

            # Compute the size of the largest measure number.
            measureSize := radix(track[-1].time / ppm, 10).count();

            # Compute the size of the beat number and the divisor (the number
            # of beats per measure that the tracker displays). 'beatPulses' is
            # the number of pulses per "beat" as displayed on the tracker.
            int beatSize, beatPulses, beatsPerMeasure;
            String beatDivisor;
            if (ppm % pps) {
                # Pulses per step doesn't evenly divide the measure.  Display
                # the beat number as beat/ppm, where beat will just be time %
                # start_of_measure (so beatPulses = 1).
                beatPulses = 1;
                beatsPerMeasure = ppm;
                beatDivisor = radix(ppm, 10);
                beatSize = beatDivisor.count();
            } else {
                # Pulses per step evenly divides the measure.
                beatPulses = pps;
                beatsPerMeasure = ppm / pps;
                beatDivisor = radix(beatsPerMeasure, 10);
                beatSize = beatDivisor.count();
            }

            __selectedEvent = null;
            for (int i = __top; i < __top + height && i < steps; ++i) {
                iter := track.iter();
                pos := i * pps;
                endPos := pos + pps;
                col := 0;
                row := i - __top;

                battr := i == __curRow ? selectedAttr : 0;

                # Write the measure and note.
                gc.write(col, row, timeAttr | battr,
                         zpad(pos / ppm, measureSize));
                col += measureSize;
                gc.write(col++, row, punctAttr | battr, ',');
                gc.write(col, row, timeAttr | battr,
                         zpad((pos % ppm) / beatPulses, beatSize));
                col += beatSize;
                gc.write(col++, row, punctAttr | battr, '/');
                gc.write(col, row, timeAttr | battr, beatDivisor);
                col += beatSize + 1;

                colIndex := 0;
                MidiEvent lastEvent;
                for (iter.setPos(pos); iter && iter.getPos() < endPos;
                     iter.next()) {
                    selected := i == __curRow && colIndex == __curCol;
                    lastEvent = iter.elem();
                    if (selected)
                        __selectedEvent = lastEvent;
                    __renderEvent(gc, row, col, lastEvent, selected);
                    col += 6;
                    colIndex++;
                }

                # If selected column is beyond the last column, fix the
                # selection and rewrite the last column with the correct
                # attribute.
                if (i == __curRow && colIndex && __curCol >= colIndex) {
                    col -= 6;
                    __renderEvent(gc, row, col, lastEvent, true);
                    __curCol = colIndex - 1;
                    __selectedEvent = lastEvent;
                }

            }
        }
    }
}

alias TrackerObj = ObjOf[Tracker];
alias TrackObj = ObjOf[OmniTrack];

@function TrackerObj makeTracker(Window parent, Track track) {
    tracker := Tracker(parent, track);

    @function(Tracker) int pageUp() {
        self.pageUp();
        return 1;
    }

    @function(Tracker) int pageDown() {
        self.pageDown();
        return 1;
    }

    @function(Tracker) int up() {
        self.up();
        return 1;
    }

    @function(Tracker) int down() {
        self.down();
        return 1;
    }

    @function(Tracker) int left() {
        self.left();
        return 1;
    }

    @function(Tracker) int right() {
        self.right();
        return 1;
    }

    @function(Tracker) MidiEvent selectedEvent() {
        return self.selectedEvent;
    }

    @function(Tracker) int setFocus(int focus) {
        self.setFocus(focus);
        return 1;
    }

    @function(Tracker) MutMidiTrackObj getTrack() {
        return makeMutMidiTrackObj(self.track);
    }

    @function(Tracker) int setTrack(Track track) {
        self.track = track;
        return 0;
    }

    result := TrackerObj(tracker);
    result.define('bind', BindCmd[Tracker](tracker, result));
    result.define('setFocus', setFocus(tracker));
    result.define('pageUp', pageUp(tracker));
    result.define('pageDown', pageDown(tracker));
    result.define('up', up(tracker));
    result.define('down', down(tracker));
    result.define('left', left(tracker));
    result.define('right', right(tracker));
    result.define('selectedEvent', selectedEvent(tracker));
    result.define('getTrack', getTrack(tracker));
    result.define('setTrack', setTrack(tracker));
    return result;
}
